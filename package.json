{
  "name": "market-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "if [ \"$VERCEL_PROJECT_NAME\" = \"market-admin-panel\" ]; then cd packages/admin && npm install && npm run build; elif [ \"$VERCEL_PROJECT_NAME\" = \"market-app-client\" ]; then cd packages/client && npm install && npm run build; else pnpm -r run build; fi",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
